:80

log

route {
   # Basic Authentication for other paths
    handle {
        basicauth /* {
            #BASICAUTH_PLACEHOLDER#
        }

        root * /app/public
        php_fastcgi unix//tmp/php-fpm.sock {
            trusted_proxies private_ranges
        }
        encode zstd gzip
        file_server
    }
}
